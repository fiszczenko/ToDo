<% if can? :create, Task %>
	<%= form_for([list, list.tasks.build], remote: true) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :content %>
	<%= f.text_field :content %>

	<%= f.submit "Add task", class: "btn btn-success" %>
<% end %>
<% end %>

<%= subscribe_to "kutas" %>
