<%= form_for([list, list.tasks.build]) do |f| %>
	<%= f.label :title %><br />
	<%= f.text_field :title %><br />

	<%= f.label :content %><br />
	<%= f.text_field :content %><br />

	<%= f.submit "Add task" %>
<% end %>
